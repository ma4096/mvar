#align(left)[// table generated via toTypstTable from makeAbbrev.py on 2025-02-09 00:11:40.578737
#table(
    columns: (13%,10%,10%,67%),
    stroke: (x,y) => (
            top: if (y != 0) {{1pt}},
            
        ),
    
    
    table.header([*Bezeichner*],[*Wert*],[*Einheit*],[*Beschreibung*]),
	[b],[2.568],[m/s],[Geschwindigkeit des Balls],
	[c],[45],[-],[-],
	[f],[1],[e],[-],
	[t],[0],[kg],[-],

)]